# =============================================================================
# PROJECT:      SteamMachine-DIY - Dynamic Configuration Examples
# VERSION:      1.1.0 - Python
# DESCRIPTION:  Full Gamescope & Environment Manifesto (Example)
# PHILOSOPHY:   KISS (Keep It Simple, Stupid)
# REPOSITORY:   https://github.com/dlucca1986/SteamMachine-DIY
# PATH:         $HOME/.config/steamos_diy/config.example
# LICENSE:      MIT
# =============================================================================

# --- [ 🟢 HOW TO EDIT ] ---
# 1. ATTIVARE: Rimuovi il carattere '#' davanti a una riga.
# 2. DISATTIVARE: Aggiungi il carattere '#' all'inizio della riga.
# 3. VALORI: Se una riga ha un valore (es. -W 1920), modificalo a piacere.
# 4. NUOVE FLAG: Se Gamescope si aggiorna, aggiungi la nuova flag in fondo.
# =============================================================================

# --- [ 1. SYSTEM ENVIRONMENT ] ---
# Variabili iniettate nella sessione Steam (Game Mode) e nei giochi (sdy).
INTEL_DEBUG=noccs
STEAM_GAMESCOPE_VRR_SUPPORTED=1
MANGOHUD=0

# --- [ 2. GLOBAL GAME DEFAULTS (SDY) ] ---
# Impostazioni base usate da 'sdy' per tutti i giochi.
# Possono essere sovrascritte dai file in games.d/
GAME_WRAPPER="gamemoderun"
GAME_EXTRA_ARGS=""

# --- [ 3. GENERAL GAMESCOPE OPTIONS ] ---
# Risoluzione e Proporzioni
-W 1920
-H 1080
#-w 1280
#-h 720

# Upscaling e Filtri
-S fit
-F fsr
--sharpness 2

# Performance e Latenza
--rt
--immediate-flips
--backend auto

# HDR e Funzioni Avanzate
#--hdr-enabled
#--adaptive-sync

# Overlay e Debug
#--mangoapp
#--debug-hud

# --- [ 4. FUTURE PROOFING: AGGIUNTA MANUALE ] ---
# Se Gamescope introduce nuove opzioni (es. --new-flag), puoi scriverle qui:
# --new-flag
# --custom-option value

# =============================================================================
# --- [ 📘 DOCUMENTATION & RULES ] ---
# =============================================================================

# 1. FUNZIONAMENTO DEI COMMENTI (#)
# -----------------------------------------------------------------------------
# Tutto ciò che inizia con '#' è invisibile al Launcher. 
# Se vuoi cambiare risoluzione da 1080p a 720p, commenta la riga '-W 1920' 
# e decommenta (togli il #) la riga '-W 1280'.

# 2. LOGICA DI PARSING
# -----------------------------------------------------------------------------
# Il Launcher e 'sdy' distinguono automaticamente:
# - Riga con '=': Viene impostata come Variabile d'Ambiente.
# - Riga senza '=': Viene passata come Argomento a Gamescope.

# 3. COMPATIBILITÀ FUTURA (Esempio Pratico)
# -----------------------------------------------------------------------------
# Questo sistema è "indipendente". Se tra un anno Gamescope aggiunge una 
# funzione ipotetica chiamata '--ultra-latency' con valore 'on', non devi 
# aspettare che noi aggiorniamo il codice Python del Launcher.
#
# Ti basterà aggiungere manualmente questa riga in fondo al file:
# --ultra-latency on
#
# Al prossimo riavvio di Steam, il Launcher la leggerà e la userà subito.

# 4. ESEMPIO RISULTANTE (Sessione Steam)
# -----------------------------------------------------------------------------
# Con le impostazioni attive sopra (senza #), il comando finale sarà:
# gamescope -W 1920 -H 1080 -S fit -F fsr --sharpness 2 --rt --immediate-flips --backend auto -- steam ...

# --- END OF EXAMPLES ---

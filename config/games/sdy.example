# =============================================================================
# SteamMachine-DIY - SDY Game Profile Template
# =============================================================================
# Path:        ~/.config/steamos-diy/games/YourGameName.conf
# Version:     3.2.4
# Description: Per-game environment variables and command injection.
# Repository:  https://github.com/dlucca1986/SteamMachine-DIY
# License:     MIT
# =============================================================================
# --- [ 🚂 HOW SDY EXECUTES YOUR GAME ] ---
# Imagine your game command as a train. This file adds wagons to it.
# The final order is:
# [Global Pre] + [Game Pre] + 🚂 GAME.exe + [Global Args] + [Game Args]
# -----------------------------------------------------------------------------

# --- [ 1. EXTRA TOOLS (Left Side of the Train) ] ---
# 💡 REAL LIFE ANALOGY: "The Equipment" 🎒
# Think of this as putting on your gear BEFORE you start the mission.
# If you need binoculars (MangoHud) or special boots (GameMode), you 
# "wear" them here so they are ready when the game starts.
#
# CUMULATIVE: [Global Pre] + [Game Pre] + Game.exe
# -----------------------------------------------------------------------------
# ✅ CORRECT SYNTAX:
# export GAME_PRE_COMMAND="mangohud gamemoderun"           # Combined tools
# export GAME_PRE_COMMAND="DXVK_HUD=fps"                   # Env variables
# -----------------------------------------------------------------------------
export GAME_PRE_COMMAND=""


# --- [ 2. GAME OPTIONS (Right Side of the Train) ] ---
# 💡 REAL LIFE ANALOGY: "The Instructions" 📝
# Think of these as specific orders you give to the game once it's running.
# Example: "Don't show the intro movie" (-novid) or "Use this engine" (-vulkan).
# Since these are at the END, they are the LAST orders the game hears.
#
# CUMULATIVE: Game.exe + [Global Args] + [Game Args]
# -----------------------------------------------------------------------------
# ✅ CORRECT SYNTAX:
# export GAME_ARGS="-novid -high"                          # Multiple flags
# export GAME_ARGS="-vulkan"                               # Single flag
# -----------------------------------------------------------------------------
export GAME_ARGS=""


# =============================================================================
# ! QUICK RECAP !
# =============================================================================
# ❓ "What is PRE-COMMAND?"
#    It's EXTERNAL. Tools that watch or help the game from the outside.
#    Analogy: Wearing a watch or a heart rate monitor while you run.
#
# ❓ "What is GAME-ARGS?"
#    It's INTERNAL. Commands that change how the game engine itself works.
#    Analogy: Telling a runner to "run faster" or "breathe deeply".
#
# ❓ "Which one should I use?"
#    - Use PRE if you want to see FPS (MangoHud) or boost CPU (GameMode).
#    - Use ARGS if the game itself needs a fix (Windowed mode, No-Intro).
# =============================================================================

# =============================================================================
# ! HINTS & TIPS !
# =============================================================================
# - Use 'global.conf' for: Always-on tools (MangoHud, GameMode).
# - Use 'game.conf'   for: Specific overrides (-opengl, --skip-intro).
# - Use quotes "" always: Prevents Bash from breaking on spaces.
# - NO SPACES around '=': Use VAR="val", NOT VAR = "val".
# =============================================================================

# =============================================================================
# ! DEBUGGING & LOGS !
# =============================================================================
# Check /var/log/steamos-diy.log for:
#   - [DEBUG]: The full final command-line exactly as executed.
# =============================================================================
